include "application.ssl.conf"

akka {
  pollers {
    uk-access-code-notifier {
      initialDelay = 0s
      interval = 1s
    }
    application-status {
      initial-delay = 0s
      interval = 1s
    }
  }
}