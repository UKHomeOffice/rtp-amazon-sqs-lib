include "application"

akka {
  stdout-loglevel = "warning"
  loglevel = "warning"
  pollers {
    uk-access-code-notifier {
      initialDelay = 0 seconds
      interval = 1 second
    }
    application-status {
      initial-delay = 0 seconds
      interval = 1 second
    }
    update-enrollment-status {
      initial-delay = 0 seconds
      interval = 1 second
    }
  }
}