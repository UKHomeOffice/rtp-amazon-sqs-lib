akka {
  stdout-loglevel = "error"
  loglevel = "error"
}

queues {
  test-queue = {
    // All optional
    // defaultVisibilityTimeout = 10 seconds
    // delay = 5 seconds
    // receiveMessageWait = 5 seconds
  }

  test-queue-error = {
    // All optional
    // defaultVisibilityTimeout = 10 seconds
    // delay = 5 seconds
    // receiveMessageWait = 5 seconds
  }
}